<%= form_with(model: avaliacao) do |form| %>
  <% if avaliacao.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(avaliacao.errors.count, "error") %> prohibited this avaliacao from being saved:</h2>

      <ul>
        <% avaliacao.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :pontos, style: "display: block" %>
    <%= form.number_field :pontos %>
  </div>

  <div>
    <%= form.label :observacoes, style: "display: block" %>
    <%= form.text_field :observacoes %>
  </div>

  <div class="field">
    <%= form.label :aluno_id %>
    <%= form.collection_select(:aluno_id, Aluno.all, :id, :nome,{:selected => @avaliacao.aluno_id})%>
  </div>
>

  <div class="field">
    <%= form.label :atividade_id %>
    <%= form.collection_select(:atividade_id, Atividade.all, :id, :titulo,{:selected => @avaliacao.atividade_id})%>
  </div>


  <div>
    <%= form.submit %>
  </div>
<% end %>
